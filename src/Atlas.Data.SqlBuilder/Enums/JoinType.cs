namespace Atlas.Data.SqlBuilder.Enums
{
    public enum JoinType
    {
        /// <summary>
        /// INNER JOIN
        /// </summary>
        Inner,

        /// <summary>
        /// LEFT INNER JOIN
        /// </summary>
        Left,

        /// <summary>
        /// RIGHT INNER JOIN
        /// </summary>
        Right
    }
}
